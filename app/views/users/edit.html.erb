
<h2>User info</h2>
<%= form_for(@user) do |f| %>
<% if @user.errors.any? %>
	<div class="error">
		<div><%= @user.errors.count %>errors prohibited this book from being saved:</div>
			<ul>
  				<% @user.errors.full_messages.each do |message| %>
 					<li><%= message + 'error' %></li>
 				<% end %>
			</ul>
	</div>
<% end %>
			<h4>Name</h4><%= f.text_field :name %>

			<h4>Image</h4><%= f.attachment_field :profile_image, placeholder: "プロフィール画像", class: "user-edit-form" %>

			<h4>Introduction</h4><%= f.text_field :introduction %>
			<br>
			<%= f.submit 'Update User'%>
<% end %>
